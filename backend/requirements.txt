fastapi
uvicorn[standard]
python-dotenv
supabase
anthropic
httpx
pydantic
apscheduler

# Agents
praw
exa-py
tavily-python

# Embeddings
openai

# Auth
pyjwt
bcrypt

# Scraping — Crawl4AI omitted on deploy (needs a headless browser; we use Jina/trafilatura,
# which is what every run has actually used). Re-add only with a Docker image + Playwright.
trafilatura
lxml
